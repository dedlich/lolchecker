"""Game-state derivation services.

Per-feature pure functions that take an LCDA snapshot or its parts
and produce derived numbers / structures. Stateless — each module
exposes pure functions, no global state, no Qt.
"""
